import React from 'react';

const BotonNavegar = (props) => ( <button onClick={props.onClick} className={props.className} valor={props.valor} name={props.name} id={props.id}></button>)

export default BotonNavegar;